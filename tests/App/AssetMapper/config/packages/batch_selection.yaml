persistent_preference:
    managers:
        default:
            normalizer: 'persistent_preference.normalizer.object'
            identifier_path: 'id'
            storage: 'persistent_preference.storage.session'
        scalar:
            normalizer: 'persistent_preference.normalizer.scalar'
        array:
            normalizer: 'persistent_preference.normalizer.array'
            identifier_path: 'id'
